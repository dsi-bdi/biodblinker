package:
  name: biodblinker
  version: 0.0.1

source:
  fn:  biodblinker-0.0.1.tar.gz
  url: ../dist/biodblinker-0.0.1.tar.gz

requirements:
  build:
    - python
    - setuptools
    - requests
    - tqdm
  run:
    - python
    - tqdm
    - requests
